import styled from "styled-components";
import {motion} from 'framer-motion'

export const HeadingH1Styled = styled(motion.h1) `
  font-size: calc(3rem + 2vmin);
  letter-spacing: 5px;
  font-weight: 800;

`